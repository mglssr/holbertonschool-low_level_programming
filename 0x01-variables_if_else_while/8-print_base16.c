#include <stdio.h>

/**
* main- Return Point
*
*Return: Always 0 (Success)
*
*/
int main(void)
{
		int n;

		for (n = 30; n <= 39 ; n++)
			{
		putchar(n);
			}
			for (n = 'a'; n <= 'f'; n++)
			{
				putchar(n);
			}
				printf("\n");
					return (0);
}
