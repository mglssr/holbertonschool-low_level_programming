#include <string.h>
#include "main.h"
/**
* _strlen - función que devuelve el largo de una string
* @s: puntero s
* Return: (0)
*/
int _strlen(char *s)
{
	size_t strlen(const char *s);
	return (strlen(s));
}

